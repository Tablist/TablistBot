module.exports = (guild, user) => {
    guild.defaultChannel.send(`${user.username} has been banned. R.I.P :c. Take a screenshot of this and send it to Twilight or Humanoid if you think this ban was unfair on behalf of the banned user.`);
  };